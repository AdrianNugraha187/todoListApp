services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: react-pnpm-app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
